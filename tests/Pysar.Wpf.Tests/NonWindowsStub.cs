namespace Pysar.Wpf.Tests;

/// <summary>
///     Empty compile unit so the project targets net10.0 on non-Windows hosts without WPF sources -
///     the same placeholder src/Pysar.Wpf keeps, for the same reason. The tests beside it
///     only build and run on Windows.
/// </summary>
internal static class NonWindowsStub;
